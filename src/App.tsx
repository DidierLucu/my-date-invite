import React from 'react';
import Envelope from './components/Envelope';

const App: React.FC = () => {
  return (
    <div className="app">
      <Envelope />
    </div>
  );
};

export default App;
